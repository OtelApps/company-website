# company-website
A modern landing page for OtelApps, highlighting our solutions and company info.

## Development

1. Use Node 22 (recommended):
   - `nvm use` (uses `.nvmrc`)
2. Install dependencies:
   - `npm install`
3. Start dev server:
   - `npm run dev`
